# LEGACY Pro Wrestling 8.0.15 — Feud-Origin VS Baseline Fix

Focused change only:

- The feud-origin VS badge is now pinned directly to the bottom edge of the wrestler portrait row.
- Placement is enforced in the rendered markup as well as CSS so older alignment rules cannot pull it upward.

Test the New Month feud-origin screen with several wrestler combinations.
