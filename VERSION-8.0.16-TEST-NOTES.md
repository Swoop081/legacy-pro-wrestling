# LEGACY Pro Wrestling 8.0.16 — Verified Feud-Origin VS Fix

The previous patches were changing the wrong VS element. This patch targets the actual feud-origin markup.

- Adds a dedicated class directly to the feud-origin VS badge.
- Pins the badge to the bottom edge of the wrestler portrait row.
- Removes the accidental feud-origin positioning from the normal Career match screen.
- Advances cache/version identifiers to 8.0.16.
