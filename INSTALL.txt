LEGACY Pro Wrestling v8.2.5 Build 1 — Outcome Continue Hotfix

Copy all files in this patch over the matching files in the current project, preserving the existing assets folders and root-level .nojekyll file.

Replace:
- game.js
- styles.css
- index.html
- service-worker.js
- version.json

After deployment, refresh the page. On iPhone/PWA, fully close and reopen the app if the previous cached build appears.
