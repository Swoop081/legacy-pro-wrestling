LEGACY Pro Wrestling 9.1.6 hotfix
Changed files only.

Fixes:
- Rebuilds the Career Hub logo and Main Menu/Career Menu controls as a stable flex header.
- Replaces the active wrestler portrait on Career match previews using the canonical portrait asset/fallback chain.
- Makes show transition bumpers reuse the compact show-intro logo proportions.
